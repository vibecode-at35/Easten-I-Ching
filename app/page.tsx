import { QuestionEntry } from "../components/QuestionEntry";

/**
 * Landing page — the question entry screen, Task 4 of M3
 * (docs/TASKS/milestone-3-reading-experience.md §6.1). Not wired to the
 * casting ceremony yet (Task 5) — QuestionEntry just captures the question
 * and signals readiness.
 */
export default function HomePage() {
  return <QuestionEntry />;
}
